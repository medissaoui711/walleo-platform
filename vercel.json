{
  "version": 2,
  "builds": [
    {
      "src": "web/**",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "web/dist",
        "buildCommand": "cd web && npm install && npx --no-install tsc && npx --no-install vite build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/web/$1"
    }
  ]
}